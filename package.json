{
  "name": "character-matters-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Character Matters Video & eBook Platform Monorepo Workspace",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "dev:landing": "npm --prefix landing run dev",
    "dev:all": "npx concurrently \"npm --prefix backend run dev\" \"npm --prefix web run dev\" \"npm --prefix landing run dev\"",
    "dev:web": "npm --prefix web run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix backend run build && npm --prefix web run build && npm --prefix landing run build",
    "install:all": "npm --prefix backend install && npm --prefix web install && npm --prefix landing install",
    "prisma:generate": "npm --prefix backend run prisma:generate",
    "prisma:deploy": "npm --prefix backend run prisma:deploy"
  },
  "workspaces": [
    "backend",
    "web",
    "landing"
  ]
}
